@inject Microsoft.AspNetCore.Http.IHttpContextAccessor httpContextaccessor
@model User

@{
    ViewData["Title"] = "Login";
}

<body>
    @using (Html.BeginForm("CheckRole", "Home", FormMethod.Post))
    {
        <div class="jumbotron" style="border:1px solid gray;">

            <div class="form-group text-center">
                <h2 class="col-md-12"> <u>Login </u></h2>
            </div>
            <div>&nbsp;</div>
            <div class="form-group centerlook">
                <div>
                    <label>Customer  Name: </label> &ensp;
                    <input asp-for="@Model.UserId" title="please Enter correct name" maxlength="15"  required/>
                </div>
                <div style="color:red">@Html.ValidationMessageFor(model => model.UserId)</div>
            </div>          


            <div class="loginbtn">

                <input type="submit" value="Log In" class="btn btn-primary" />

            </div>
        </div>
    }
</body>
